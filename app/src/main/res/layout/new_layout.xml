<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    >

    <TextView
        android:id="@+id/textView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Mohammad Lutful"
        android:gravity="center"
        android:textSize="40dp"
        android:layout_margin="10dp"

        />

    <ImageView
        android:id="@+id/imageView"
        android:layout_width="match_parent"
        android:layout_height="120dp"
        tools:srcCompat="@drawable/m" />

    <TextView
        android:id="@+id/textView2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="10dp"
        android:text="You can easily compress photo to 50kb using photo convert into 50kb tool. It’s the fastest way to compress photo size on the photo convert into 50kb tool. Just select photo that you wish to compress on the photo convert into 50kb."
        android:textStyle="bold"

        />

</LinearLayout>